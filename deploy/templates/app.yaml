apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: {{ .config.env }}-app-api
  labels:
    app: app-api
    env: {{ .config.env }}
spec:
  replicas: 1
  selector:
    app: app-api
    env: {{ .config.env }}
  triggers:
    - type: ImageChange
      imageChangeParams:
        containerNames:
          - app-api
        from:
          kind: ImageStreamTag
          name: app-api:{{ .config.env }}-latest
  template:
    metadata:
      labels:
        app: app-api
        env: {{ .config.env }}
    spec:
      containers:
        - name: app-api
          image: kscout/app-api:{{ .config.env }}-latest
          env:
            - name: APP_HTTP_ADDR
              value: ":5000"
            - name: APP_DB_USER
              valueFrom:
                configMapKeyRef:
                  name: {{ .config.env }}-db-config
                  key: mongoUser
            - name: APP_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .config.env }}-mongo-credentials
                  key: password
            - name: APP_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ .config.env }}-db-config
                  key: mongoDbName
            - name: APP_DB_HOST
              value: $({{ .config.env | strings.ToUpper }}_MONGO_SERVICE_HOST)
            - name: APP_GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .config.env }}-gh-api-configuration
                  key: apiToken
            - name: APP_GH_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .config.env }}-gh-api-configuration
                  key: webhookSecret
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP
          readinessProbe:
            httpGet:
              path: /health
              port: http
              scheme: HTTP              
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .config.env }}-app-api
  labels:
    app: app-api
    env: {{ .config.env }}
spec:
  type: ClusterIP
  selector:
    app: app-api
    env: {{ .config.env }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      name: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .config.env }}-app-api
  labels:
    app: app-api
    env: {{ .config.env }}
spec:
  host: {{ .config.host }}
  path: /
  to:
    kind: Service
    name: {{ .config.env }}-app-api
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
status:
  ingress:
    - host: api.kscout.io
